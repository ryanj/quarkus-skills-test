---
name: file-tree
description: List files and directories in a tree format using an optional relative path
---

When asked to search or browse local files, use `listFileTree("")` to browse the project root, or `listFileTree("relative/path")` to browse a subdirectory.